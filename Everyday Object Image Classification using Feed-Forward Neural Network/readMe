We are using the CIFAR10 dataset: https://www.cs.toronto.edu/~kriz/cifar.html
We will set up a training pipeline to train a neural network on a GPU (if possible) and experiment with different network architectures & hyperparameters
